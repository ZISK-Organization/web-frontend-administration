export interface submission {
  corrected: boolean;
  correctionDetails: string;
  id: number;
  moduleDetails: string;
  moduleId: number;
  moduleSection: number;
  points: number;
  submission: string;
  submissionDate: Date;
  userId: string;
}
